# mset: an OCaml library for small multisets

Implements multisets using a bitset as soon as they are small enough
to fit in one `int`.

Here is the
[documentation](https://backtracking.github.io/mset/mset/Mset/index.html).
